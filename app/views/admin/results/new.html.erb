<h1>New result</h1>

<%= form_for(:result, :url => admin_results_path) do |f| %>
  <%= render(:partial => 'form', :locals => { :f => f }) %>
  <p><%= f.submit 'Save', :class => 'btn btn-primary' %></p>
<% end %>
