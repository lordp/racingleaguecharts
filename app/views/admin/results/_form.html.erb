<%= f.label :position %>
<%= f.text_field :position %>

<%= f.label :race_id %>
<%= f.collection_select :race_id, Race.all, :id, :name_and_league %>

<%= f.label :driver_id %>
<%= f.collection_select :driver_id, Driver.all, :id, :name %>

<%= f.label :team_id %>
<%= f.collection_select :team_id, Team.all, :id, :name %>

<%= f.label :pole_position, :class => 'checkbox' do %>
  <%= f.check_box :pole_position %> Pole Position?
<% end %>

<%= f.label :fastest_lap, :class => 'checkbox' do %>
  <%= f.check_box :fastest_lap %> Fastest Lap?
<% end %>
