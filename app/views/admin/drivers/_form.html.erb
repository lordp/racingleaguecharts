<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :team %>
<%= f.collection_select :team_id, Team.all, :id, :name %>

<%= f.label :country %>
<%= f.text_field :country %>
<span class="help-block">Used for flair</span>
