<h1>Editing Session</h1>

<%= form_for(@session, :url => say_what_session_path(@session)) do |f| %>
  <%= render(:partial => 'shared/errors', :object => @session ) %>
  <%= render(:partial => 'form', :locals => { :f => f }) %>
<% end %>