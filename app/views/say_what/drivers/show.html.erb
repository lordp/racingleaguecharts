<h1><%= @driver.name %></h1>
<%= link_to('Edit', edit_say_what_driver_path(@driver)) %> | <%= link_to('Destroy', say_what_driver_path(@driver), :method => :delete, :data => { :confirm => 'Are you sure?' }) %> | <%= link_to("Back to index", say_what_drivers_path) %>
<h2>Sessions</h2>
<%= paginate @sessions %>
<% @sessions.each_with_index do |session, index| %>
  <%= render(:partial => 'sessions/session', :object => session, :locals => { :index => index }) %>
<% end %>
<%= paginate @sessions %>
