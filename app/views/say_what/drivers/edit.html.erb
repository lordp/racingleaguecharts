<h1>Editing Driver</h1>

<%= form_for(@driver, :url => say_what_driver_path(@driver)) do |f| %>
  <%= render(:partial => 'shared/errors', :object => @driver ) %>
  <%= render(:partial => 'form', :locals => { :f => f }) %>
<% end %>