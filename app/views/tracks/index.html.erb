<h1>Tracks</h1>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>Track</th>
    </tr>
  </thead>
  <tbody>
  <% @tracks.each do |track| %>
    <tr>
      <td><%= link_to(track.name, track_path(track)) %></td>
    </tr>
  <% end %>
  </tbody>
</table>
